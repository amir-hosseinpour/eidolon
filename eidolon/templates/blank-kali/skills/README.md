# blank-kali skills

This directory is mounted into the engagement workspace under `skills/`.
It is intended for Claude Code "skills" (prompts, scripts, system-level
guidance) that ship alongside the template.

The blank-kali template ships no skills by default. Operators who fork
this template can drop skill subdirectories here; each one will surface
to the laptop CC pack via the workspace's `skills/` directory.
